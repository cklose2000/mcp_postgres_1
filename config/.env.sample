# Supabase Project Configuration
# ---------------------------------

# Project URL (required)
SUPABASE_PROJECT_URL=https://your-project-ref.supabase.co

# API Key (required)
SUPABASE_API_KEY=your-supabase-api-key

# Database Configuration
# ---------------------------------

# Database password (for direct PostgreSQL connections and pooler connections)
# You can find this in Supabase Dashboard → Project Settings → Database → Connection Info
DB_PASSWORD=your_database_password_here

# Environment selection
# ---------------------------------

# Active environment (dev, test, or prod)
ACTIVE_ENV=dev

# Connection URLs (constructed using DB_PASSWORD)
# ---------------------------------
# These are provided as templates but will be constructed dynamically in code
# You shouldn't need to modify these directly

# Direct connections
# DEV_DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@db.your-project-ref.supabase.co:5432/postgres?sslmode=require
# TEST_DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@db.your-project-ref.supabase.co:5432/postgres?sslmode=require
# PROD_DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@db.your-project-ref.supabase.co:5432/postgres?sslmode=require

# Connection pooler URLs
# SESSION_POOLER_URL=postgres://postgres.your-project-ref:${DB_PASSWORD}@aws-0-us-east-1.pooler.supabase.com:5432/postgres
# TRANSACTION_POOLER_URL=postgres://postgres.your-project-ref:${DB_PASSWORD}@aws-0-us-east-1.pooler.supabase.com:6543/postgres 